apiVersion: batch/v1
kind: Job
metadata:
  name: init-posdb-job
spec:
  template:
    spec:
      containers:
        - name: init-posdb-container
          image: us-central1-docker.pkg.dev/grounded-pivot-466222-u4/my-docker-repo/init-posdb:latest
          # image: init-posdb:latest  # Use the local image built from Dockerfilekk
          imagePullPolicy: IfNotPresent
          env:
            - name: PGPASSWORD
              value: "postgres"  # or your actual DB password
      restartPolicy: Never
