apiVersion: batch/v1
kind: Job
metadata:
  name: init-bankingappdb-job
spec:
  template:
    spec:
      containers:
        - name: init-bankingappdb-container
          # image: gcr.io/YOUR_PROJECT/init-db:latest
          image: init-bankingappdb  # Use the local image built from Dockerfile
          env:
            - name: PGPASSWORD
              value: "postgres"  # or your actual DB password
      restartPolicy: Never
